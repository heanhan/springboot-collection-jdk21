package com.example.ddd.product.interfaces;

/**
 * 用户接口层 —— product-service 的协议适配。
 *
 * <p><b>子包：</b>
 * <ul>
 *   <li>{@code rest}：面向前端 / 管理端的 REST 控制器。</li>
 *   <li>{@code facade}：面向内部服务的 Feign 服务端点。</li>
 *   <li>{@code dto}：请求 / 响应 DTO。</li>
 *   <li>{@code assembler}：DTO ⇄ Domain 转换。</li>
 * </ul>
 */
