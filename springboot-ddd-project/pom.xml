<?xml version="1.0" encoding="UTF-8"?>
<!--
    ============================================================================
    springboot-ddd-project 父 POM
    ============================================================================
    本模块是一个"pom 打包"的父工程，用于组织一个模拟电商购物平台的 DDD
    (领域驱动设计) 风格多模块工程，主要面向学习目的。

    架构形态：模块化微服务
      - 每个限界上下文 (Bounded Context) 都是独立的 Maven 子模块
      - 每个子模块都是可独立启动的 Spring Boot 应用
      - 独立 MySQL Schema、独立端口
      - 同步调用：OpenFeign (HTTP)
      - 异步协作：RocketMQ (领域事件)
      - 持久化：统一 JPA / Hibernate，领域层保持纯粹 (不出现 JPA/Spring 注解)

    子模块一览：
      共享模块 (非可运行服务)：
        - ddd-common         共享内核：Money / Address / DomainEvent / Result ...
        - ddd-api-contract   对外契约：Feign Client 接口 + DTO
      业务服务：
        - ddd-auth-service        认证鉴权中心 (Spring Security + JWT 双 Token + Redis)
        - ddd-user-service        用户中心 (用户资料 / 地址簿 / RBAC)
        - ddd-product-service     商品目录 (SPU / SKU / 类目 / 品牌)
        - ddd-inventory-service   库存仓库 (仓库 / 库存预占-实扣-回滚 / 库存流水)
        - ddd-order-service       订单 (Order 聚合根 / 状态机 / 领域事件)
        - ddd-payment-service     支付 (支付单 / 退款单 / Mock 渠道)
        - ddd-logistics-service   物流履约 (发货单 / 运单 / 物流轨迹)
        - ddd-cart-service        购物车 (加购 / 结算预览)

    依赖方向约束 (在每层的 package-info.java 中都有说明)：
        interfaces  ->  application  ->  domain  <-  infrastructure
      domain 层不依赖任何其他层，是纯粹的业务模型。
    ============================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example</groupId>
        <artifactId>springboot-collection-jdk21</artifactId>
        <version>1.0</version>
    </parent>

    <groupId>com.example.ddd</groupId>
    <artifactId>springboot-ddd-project</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>

    <name>springboot-ddd-project</name>
    <description>DDD 风格购物平台学习项目 (Spring Boot 3.2 + JDK 21)</description>

    <modules>
        <!-- 共享模块 -->
        <module>ddd-common</module>
        <module>ddd-api-contract</module>
        <!-- 业务服务 -->
        <module>ddd-auth-service</module>
        <module>ddd-user-service</module>
        <module>ddd-product-service</module>
        <module>ddd-inventory-service</module>
        <module>ddd-order-service</module>
        <module>ddd-payment-service</module>
        <module>ddd-logistics-service</module>
        <module>ddd-cart-service</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- 本工程专用版本属性 (与父工程属性解耦，方便独立演进) -->
        <ddd.version>1.0</ddd.version>
        <spring-cloud.version>2023.0.0</spring-cloud.version>
        <jjwt.version>0.12.5</jjwt.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <lombok.version>1.18.30</lombok.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <springdoc.version>2.3.0</springdoc.version>
        <flyway.version>9.22.3</flyway.version>
        <mysql.version>8.2.0</mysql.version>
    </properties>

    <!-- 所有子模块共享的基础依赖：只放"人人都需要"的 -->
    <dependencies>
        <!-- Spring Boot 基础：日志 / 自动装配 / 上下文 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- 参数校验 (Jakarta Bean Validation) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Lombok：@Getter/@Builder/@Slf4j 等，父工程已提供版本 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId><artifactId>h2</artifactId><scope>test</scope>
        </dependency>

        <!-- 单元测试：JUnit 5 + AssertJ + Mockito -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- ============================================================
                 Spring Cloud BOM：提供 OpenFeign / LoadBalancer 等版本管理
                 ============================================================ -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ============================================================
                 内部模块统一版本管理
                 ============================================================ -->
            <dependency>
                <groupId>com.example.ddd</groupId>
                <artifactId>ddd-common</artifactId>
                <version>${ddd.version}</version>
            </dependency>
            <dependency>
                <groupId>com.example.ddd</groupId>
                <artifactId>ddd-api-contract</artifactId>
                <version>${ddd.version}</version>
            </dependency>

            <!-- ============================================================
                 JWT (jjwt 0.12.x)：仅在 auth 服务中使用
                 ============================================================ -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <!-- ============================================================
                 MapStruct：PO <-> Domain 对象转换 (可选，本项目也用手写转换)
                 ============================================================ -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok-mapstruct-binding</artifactId>
                <version>${lombok-mapstruct-binding.version}</version>
            </dependency>

            <!-- ============================================================
                 数据库相关：Flyway (数据库版本迁移) + MySQL 驱动
                 ============================================================ -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-mysql</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql.version}</version>
            </dependency>

            <!-- ============================================================
                 接口文档：springdoc-openapi (Knife4j 底层)
                 ============================================================ -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Spring Boot Maven 插件：打可执行 fat jar -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId><artifactId>maven-surefire-plugin</artifactId><version>3.2.5</version>
            </plugin>
            <!-- 编译器：开启 -parameters (Spring 反射需要参数名)、注解处理器 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                    <parameters>true</parameters>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
